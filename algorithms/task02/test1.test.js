describe('Algoritms', () => {
  it('test func', () => {
    const sum = (a, b) => {
      return a + b; 
    }

    let output = func()
    // console.info('[]', { output })
    output = true
    const expected = true
    expect(output).toEqual(expected)
  })
})
